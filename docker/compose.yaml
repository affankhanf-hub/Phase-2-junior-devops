version: '3.8'

services:
  web:
    build: .
    container_name: technova-web
    ports:
      - "8080:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
    environment:
      - APP_ENV=production
    restart: always
